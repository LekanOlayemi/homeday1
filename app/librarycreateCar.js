   module.exports = function(arg){

	var car = new Car();

	this.type = type
	this.model = model
	this.name = name
	this.createCar = createCar
    
    if(typeof arg === honda){
        return {}
        if(typeof arg === honda){
        	return Truthy()
        }
    }

    if(typeof arg === gm.name){
        return 'General'
        if(typeof arg === gm.model){
        	return 'GM'
        }
    }

    if(typeof arg === toyota.name){
        return 'Toyota'
        if(typeof arg === toyota.model){
        	return 'Corolla'
        }
    }

    if(typeof arg === opel.numberofdoors){
     	return 4
    }


    if(typeof arg === porshe.numberofdoors){
        return 2
        if(typeof arg === porshe.drive){
        	return 5 
        if(typeof arg === porshe.speed){
        return '250km/h'
        if(typeof arg === gm.model){
        	return 'GM'
        }
    }
        }
    }
}

